@model IEnumerable<CollegeSystemSystem.Models.Subject>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubjectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher.Name)
            </th>
            <th>
                Average Grade
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>        
                    <a asp-action="Details" asp-route-id="@item.Id" >@Html.DisplayFor(modelItem => item.SubjectName)</a>                    
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Course.CourseName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Teacher.Name)
                </td>
                <td>
                    @{
                        var sum = item.Grades.Sum(g => g.GradeNumber);
                        var count = item.Grades.Count();
                        var avg = sum / count;
                        if (count == 0)
                        {
                            avg = 0;
                        }
                    }
                    @avg
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secundary">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-alarm">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
